# NEOVIM-Environment

This environment depends on various plugins, including a plugin manager.  
Simply put `init.lua`, `after/`, and `lua/` in `$HOME/.config/nvim/` after neovim is built.  
All files in this repository cannot be used with vim.

